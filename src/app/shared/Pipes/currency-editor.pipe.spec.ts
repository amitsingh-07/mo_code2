import { CurrencyEditorPipe } from './currency-editor.pipe';

describe('CurrencyEditorPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyEditorPipe();
    expect(pipe).toBeTruthy();
  });
});
