import { CapsLockInputDirective } from './caps-lock-input.directive';

describe('CapsLockInputDirective', () => {
  it('should create an instance', () => {
    const directive = new CapsLockInputDirective();
    expect(directive).toBeTruthy();
  });
});
